dependencies:
  espressif/esp-dsp: "*"
  usb_host_uac:
    version: "1.2.*"
  espressif/usb: "*"
  espressif/usb_host_cdc_acm: "^2.2.0"
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_hosted:
    version: ">=2.0.0,<2.1.0"
    rules:
      - if: "target in [esp32p4]"
